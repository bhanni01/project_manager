# PostgreSQL — pooled connection (PgBouncer/Neon transaction mode) used for normal queries
DATABASE_URL=

# PostgreSQL — direct connection used for Prisma migrations and the FY rollover transaction
DIRECT_URL=

# Auth.js v5
AUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
